# Contributing

このリポジトリでの開発に参加する際のルールをまとめます。

## issueドリブンで作業する

- 作業を始める前に、対応する issue を作成（または既存の issue を確認）してください。
- issue には対応内容や背景、目的を記載し、作業はその issue に紐づく形で進めてください。
- issue に紐づかない場当たり的な変更は避け、変更が必要になった場合はまず issue を立ててから着手してください。
- プルリクエストの本文には、対応する issue番号を記載してください（例: `Closes #12`）。

## コミットメッセージのルール

コミットメッセージは以下の形式に従ってください。

```
<type>: <変更内容の要約>

<変更した理由>
```

### 接頭辞（type）

変更内容に応じて、以下のいずれかを1行目の先頭に付けてください。

| type | 説明 |
| --- | --- |
| `feat` | 新機能の追加 |
| `fix` | バグ修正 |
| `docs` | ドキュメントのみの変更 |
| `style` | 動作に影響しないコードスタイルの変更（フォーマットなど） |
| `refactor` | 挙動を変えないリファクタリング |
| `test` | テストの追加・修正 |
| `chore` | ビルド・依存関係・設定など上記に当てはまらない変更 |

### 理由の記載

1行目の要約に続けて、空行を挟んだ本文に「なぜその変更が必要だったか」を記載してください。何を変更したかはdiffを見れば分かるため、背景・目的・経緯など、diffだけでは伝わらない情報を書くようにしてください。

例:

```
fix: 状態ファイルが存在しない場合にキーが ERROR 表示になる不具合を修正

初回起動時など状態ファイルがまだ作成されていない場合、
IDLE ではなく ERROR と誤判定されてしまっていたため、
ファイル未存在時は IDLE として扱うよう修正した。
```
