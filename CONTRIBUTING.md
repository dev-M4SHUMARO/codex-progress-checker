# Contributing

このリポジトリでの開発に参加する際のルールをまとめます。

## Issue ドリブンで作業する

- 作業を始める前に、対応する Issue を作成（または既存の Issue を確認）してください。
- 実装内容や変更理由は、着手前に Issue へ記載し、認識をすり合わせてから作業を進めてください。
- Pull Request の本文には、対応する Issue 番号を `Closes #123` のように記載し、PR と Issue を紐付けてください。
- 対応する Issue がない変更（typo 修正などの軽微なものを除く）は、先に Issue を作成してから着手してください。

## コミットメッセージのルール

コミットメッセージの 1 行目は、変更内容を表す接頭辞から始めてください。

| 接頭辞 | 用途 |
| --- | --- |
| `feat` | 新機能の追加 |
| `fix` | バグ修正 |
| `docs` | ドキュメントのみの変更 |
| `refactor` | 挙動を変えないコードの整理・改善 |
| `test` | テストの追加・修正 |
| `chore` | ビルド・依存関係・設定など上記に当てはまらない変更 |

1 行目のあとに空行を挟み、本文にはその変更が**なぜ必要だったか**（理由・背景）を記載してください。「何をしたか」はコードの差分から分かるため、コミットメッセージには理由を優先して書いてください。

### 例

```text
fix: 状態ファイルが存在しない場合に IDLE を表示する

Codex を一度も起動していない環境では状態ファイルが存在せず、
アイコンが ERROR のまま表示されてしまっていた。
未起動時と区別できるよう、ファイル未検出時は IDLE を返すようにした。
```

```text
docs: 構成管理ルールを追加

Issue ドリブンでの進め方やコミットメッセージの書き方が
明文化されておらず、貢献者ごとに書き方がばらついていたため、
CONTRIBUING.md にルールをまとめた。
```
